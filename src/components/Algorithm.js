
export const Linear = (n) => {
  const result = [];
  for (let index = 0; index < n; index++) {
    result.push(index);
  }
    return result;
};

export const Red = (n) => {
  const result = [];
  for (let index = 0; index < n; index++) {
    result.push(index);
  }
};

